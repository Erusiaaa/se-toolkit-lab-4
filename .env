# The URL of the back-end API that the dev server proxies to.
# Default: http://127.0.0.1:42002 (local Docker Compose).
# Change to http://<your-vm-ip-address>:<api-port> if the API runs on the VM.
VITE_API_TARGET=http://10.93.25.146:42002
APP_HOST_ADDRESS=0.0.0.0
APP_HOST_PORT=42002
APP_CONTAINER_ADDRESS=0.0.0.0
APP_CONTAINER_PORT=8000
CADDY_CONTAINER_PORT=80
API_TOKEN=my-secret-api-key